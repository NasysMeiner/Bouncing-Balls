using UnityEngine;

public class Trail : MonoBehaviour
{
    //[SerializeField] private Ball _ball;

    //private Quaternion _target;

    //private void Update()
    //{
    //    ChangeRotation();
    //}

    //private void ChangeRotation()
    //{
    //    if(_ball.Rigidbody.velocity.magnitude != 0)
    //    {
    //        _target = Quaternion.LookRotation(new Vector3(-_ball.Rigidbody.velocity.x, -_ball.Rigidbody.velocity.y, -_ball.Rigidbody.velocity.z));
    //        transform.rotation = _target;
    //    }
    //}
}
